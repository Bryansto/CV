function myFunction() {
  var yourname = prompt('What is your name?', 'type name here').toLowerCase();
  alert('Welcome to my page ' + + yourname.slice(0,1).toUpperCase() + yourname.slice(1,name.length) + '!');
}

function myFalso() {
  var yourname = prompt('This is the same but different', 'type name here').toLowerCase();
  alert('Welcome to my page ' + + yourname.slice(0,1).toUpperCase() + yourname.slice(1,name.length) + '!');
}
